# Sin pipeline propio: llama a los reusable workflows de fixcomap/platform.
#   PR       → checks + preview <rama>.fixcomap-agustin.pages.dev  (environment preview)
#   develop  → checks + preview develop.fixcomap-agustin.pages.dev (environment preview)
#   main     → checks + producción agustin.fixcomap.com            (environment production)
name: ci

on:
  pull_request:
    branches: [develop, main]
  push:
    branches: [develop, main]
  workflow_dispatch:

permissions: {}

concurrency:
  group: ci-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: false

# Un workflow llamado no puede pedir más permisos que el job que lo llama.
jobs:
  checks:
    permissions:
      contents: read
    uses: fixcomap/platform/.github/workflows/rw-static-checks.yml@main
    with:
      site_dir: public

  deploy:
    needs: checks
    permissions:
      contents: read
    uses: fixcomap/platform/.github/workflows/rw-pages-deploy.yml@main
    with:
      project_name: fixcomap-agustin
      site_dir: public
      environment: ${{ github.ref_name == 'main' && 'production' || 'preview' }}
      production_url: https://agustin.fixcomap.com
    secrets: inherit
