Ficheros que faltan (no se versionan placeholders; súbelos aquí con estos nombres exactos):

- agustin.jpg     — foto cuadrada, ~600×600 px, < 200 KB
- cv-agustin.pdf  — sin teléfono, dirección ni DNI: la web es pública e indexable

Cloudflare Pages sirve el directorio `public/` tal cual; sin build.
